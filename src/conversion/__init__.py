"""Format-independent conversion core.

This package contains conversion plans, field mapping, and execution pipelines. It depends only on the
shared source model and writes through target writers, so source-specific parsing stays in adapters.
"""

__all__: list[str] = []
